- if @tweets.any?
  .actions.get-new-tweets
    = render 'tweets/link_to_new_tweets', since_id: @tweets.first.id

  = render 'tweets/tweets', tweets: @tweets

  .actions.get-more-tweets
    = render 'link_to_tweets', max_id: @tweets.last[:id], tweets: @tweets

